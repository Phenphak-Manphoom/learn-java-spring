spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: okyuki46
    password: cbkrfciphsapqvyn
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/backend
    username: postgres
    password: postgres
  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
      max-request-size: 5MB

app:
  token:
    secret: secret,
    issuer: BackendService
  email:
    from: okyuki46@gmail.com
